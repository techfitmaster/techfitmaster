# 任务看板

## 🔴 P0 - 紧急

```dataview
TASK
WHERE contains(file.name, "项目") OR contains(file.name, "Tasks")
WHERE !completed
WHERE contains(text, "P0")
```

---

## 🟠 P1 - 重要

```dataview
TASK
WHERE contains(file.name, "项目") OR contains(file.name, "Tasks")
WHERE !completed
WHERE contains(text, "P1")
```

---

## 🟡 P2 - 正常

```dataview
TASK
WHERE contains(file.name, "项目") OR contains(file.name, "Tasks")
WHERE !completed
WHERE contains(text, "P2")
```

---

## 🟢 P3 - 可选

```dataview
TASK
WHERE contains(file.name, "项目") OR contains(file.name, "Tasks")
WHERE !completed
WHERE contains(text, "P3")
```

---

## ✅ 最近完成

```dataview
TASK
WHERE completed
WHERE file.mtime >= date(today) - dur(7 days)
LIMIT 10
```

---

#任务看板
